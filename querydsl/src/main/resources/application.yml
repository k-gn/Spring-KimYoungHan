spring:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
        use_sql_comments: true # jpql 확인

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace